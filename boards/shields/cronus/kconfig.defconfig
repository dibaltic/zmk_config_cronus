if SHIELD_CRONUS_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Cronus"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_CRONUS_LEFT || SHIELD_CRONUS_RIGHT

config ZMK_SPLIT
    default y

endif
